# Textcat 
### textcat v3 by zion8992
This repository it only textcat *server*, client is available here.

## Running
To run textcat, download the binaries from the *releases* tab.
You can also build from source:

- you must have go installed (go.dev)

`go build -o textcat cmd/main.go` -> builds textcat server binary

## Command Line Options
